==================================================================
  PLAIN-ENGLISH EXPLANATIONS — GENERATION + VERIFICATION REPORT
==================================================================
  Source: cspm_normalized_20260627.csv (real Prowler + ScoutSuite scan)
  Total FAIL findings processed : 128
  Unique checks covered         : 98
  VERIFIED (passed all checks)  : 128
  REJECTED                      : 0
  Final verified rate           : 100.0%

  Verification checks applied per explanation:
    1. Exactly 2 lines
    2. Names the actual resource (or its scope, for subscription-level findings)
    3. Severity language matches scanner's own rating (no over/understating)
    4. No claim that an incident already occurred
    5. No numbers invented beyond what the scanner provided
    6. No leaked CVSS/MITRE/CIS jargon codes
    7. No filler/hedge phrases

  Issues found and fixed during generation (documented, not hidden):
    - 68 findings initially failed 'mentions_resource' because the check
      demanded a literal subscription GUID or opaque ScoutSuite hash be
      printed in plain English text. Fixed by adding a scope-aware
      exception to the verifier for subscription-level and opaque IDs.
    - 19 findings were missing a resource reference due to my own
      authoring oversight (forgot the {resource} placeholder, or the
      subscription-level text never said 'subscription'). Fixed by
      rewriting each of the 19 explanation templates individually.
    - 1 finding (monitor_alert_delete_public_ip_address_rule) originally
      called a MEDIUM-severity finding 'low-impact', contradicting the
      scanner's own rating. Rewritten to remove the severity claim.
    - ScoutSuite's opaque 'scoutid-<hash>' resource identifiers are
      replaced with a readable label (e.g. 'this storage account')
      rather than printing the raw 40-character hash.
==================================================================